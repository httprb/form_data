inherit_from:
  - .rubocop/layout.yml
  - .rubocop/metrics.yml
  - .rubocop/rspec.yml
  - .rubocop/style.yml

plugins:
  - rubocop-rake
  - rubocop-rspec

AllCops:
  DisplayCopNames: true
  NewCops: enable
  TargetRubyVersion: 3.2
